// Participant pages - Mobile-first study participation interface
// Participants can:
// - Complete enrollment flow (registration, consent, screening)
// - View their study dashboard and progress
// - Complete PRO (Patient-Reported Outcome) surveys
// - See completion status (but not scores until study ends)

export default function StudyPage() {
  return (
    <div>
      <h1>Study Participant Portal</h1>
    </div>
  )
}
